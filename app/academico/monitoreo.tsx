import { ScreenPlaceholder } from "@/src/components/ScreenPlaceholder";
import { SCREENS } from "@/src/data/screens";

export default function MonitoreoAcademicoScreen() {
  return <ScreenPlaceholder screen={SCREENS.monitoreoAcademico} />;
}
