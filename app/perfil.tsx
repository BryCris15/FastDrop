import { ScreenPlaceholder } from "@/src/components/ScreenPlaceholder";
import { SCREENS } from "@/src/data/screens";

export default function PerfilScreen() {
  return <ScreenPlaceholder screen={SCREENS.perfil} />;
}
